# Copy this file to terraform.tfvars and fill in your values
# Or run ./setup.sh for interactive configuration

app_name = "your-org-agenda-app"

# Optional overrides
# region            = "ord"  # Fly.io region (default: ord/Chicago)
# vm_size           = "shared-cpu-1x"
# vm_memory         = 512
# git_sync_interval = 60

# Custom elisp (optional)
# custom_elisp = <<-EOT
#   (setq org-agenda-api-capture-templates
#     '(("todo" :name "Todo" :template ("t" "Todo" entry (file "/data/org/inbox.org") "* TODO %^{Title}\n%U\n%?"))))
# EOT

# Note: Secrets (GIT_SYNC_REPOSITORY, GIT_SSH_PRIVATE_KEY, AUTH_USER,
# AUTH_PASSWORD, etc.) are managed via agenix encryption and set using
# flyctl secrets in deploy.sh. Do not add them here.
