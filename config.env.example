# config.env.example - Copy to config.env and fill in your values
# Alternative: Run ./setup.sh for interactive configuration

# === Fly.io Configuration ===
FLY_APP_NAME="your-org-agenda-app"
FLY_REGION="ord"  # See: https://fly.io/docs/reference/regions/

# === Authentication ===
AUTH_USER="your-username"
# Note: AUTH_PASSWORD is stored encrypted in secrets/auth-password.age
# It gets decrypted automatically when you enter the dev shell (nix develop)

# === Git Sync Configuration ===
# Repository containing your org files (SSH URL for private repos)
GIT_SYNC_REPOSITORY="git@github.com:your-username/your-org-repo.git"
GIT_USER_EMAIL="org-agenda-api@example.com"
GIT_USER_NAME="org-agenda-api"

# === SSH Key for agenix Secrets ===
# Path to your SSH private key (ed25519) for encrypting/decrypting secrets
# setup.sh can generate a dedicated deploy key for you
SSH_IDENTITY_FILE="$HOME/.ssh/id_ed25519"
