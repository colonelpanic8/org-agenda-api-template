# OpenTofu / Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!terraform.tfvars.example

# User configuration (generated by setup.sh)
config.env
secrets.nix

# Secrets (decrypted or private keys)
*.pem
*.key
secrets/deploy-key
secrets/deploy-key.pub

# Nix
result
result-*

# direnv
.direnv/
